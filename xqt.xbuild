# YAML builder definition
type: PackageBuilder

# define the python location
package:
    name: xqt
    path: ./src

# define global settings
settings:
    distributionName: projex_xqt

# define the path options
paths:
    buildPath: ./.build/{platform}
    distributionPath: ./.dist/{platform}
    outputPath: ./bin

